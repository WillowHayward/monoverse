import {
    addProjectConfiguration,
    formatFiles,
    generateFiles,
    Tree,
    ProjectGraph
} from '@nx/devkit';
import * as path from 'path';
import { CollectGeneratorSchema } from './schema';

export async function collectGenerator(
    tree: Tree,
    options: CollectGeneratorSchema
) {

}

export default collectGenerator;
