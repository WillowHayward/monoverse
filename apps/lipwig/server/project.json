{
    "name": "lipwig-server",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/lipwig/server/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nrwl/webpack:webpack",
            "outputs": ["{options.outputPath}"],
            "options": {
                "target": "node",
                "compiler": "tsc",
                "outputPath": "dist/apps/lipwig/server",
                "main": "apps/lipwig/server/src/main.ts",
                "tsConfig": "apps/lipwig/server/tsconfig.app.json",
                "assets": ["apps/lipwig/server/src/assets"]
            },
            "configurations": {
                "production": {
                    "optimization": true,
                    "extractLicenses": true,
                    "inspect": false,
                    "fileReplacements": [
                        {
                            "replace": "apps/lipwig/server/src/environments/environment.ts",
                            "with": "apps/lipwig/server/src/environments/environment.prod.ts"
                        }
                    ]
                }
            }
        },
        "serve": {
            "executor": "@nrwl/js:node",
            "options": {
                "buildTarget": "lipwig-server:build"
            },
            "configurations": {
                "production": {
                    "buildTarget": "lipwig-server:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nrwl/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/lipwig/server/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nrwl/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/lipwig/server/jest.config.ts",
                "passWithNoTests": true
            }
        }
    },
    "tags": []
}
