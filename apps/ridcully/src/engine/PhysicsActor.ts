/*import * as BABYLON from 'babylonjs';
import { Actor } from './Actor';
import { Stage } from './Stage';

export class PhysicsActor extends Actor implements BABYLON.IPhysicsEnabledObject {
  //public position: BABYLON.Vector3;

  constructor(name: string, scene: Stage) {
    super(name, scene);
  }

  public getBoundingInfo() {
    return this.root.getBoundingInfo();
  }

  public getVerticesData(kind: string, copyWhenShared?: boolean, forceCopy?: boolean): BABYLON.Nullable<BABYLON.FloatArray> {
    return this.root.getVerticesData(kind, copyWhenShared, forceCopy);
  }

  public get position(): BABYLON.Vector3 {
    return this.root.position;
  }

}*/
